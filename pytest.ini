[pytest]
# Asyncio strict mode + explicit fixture loop scope (removes deprecation warning)
asyncio_mode = strict
asyncio_default_fixture_loop_scope = function

# Discover tests here
testpaths = tests

# CLI output
addopts = -ra -q
log_cli = true
log_cli_level = INFO
cache_dir = .pytest_cache
